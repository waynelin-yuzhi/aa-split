-- ============================================================
-- AA 分帳 — covers 上傳上限再收緊 1.5MB → 512KB（2026-06-18）
-- 前端封面已改「壓到 ~200KB」（downscaleImage targetKB），正常上傳遠低於此；
-- 把硬上限降到 512KB＝直接打 API 灌圖時每檔最多 512KB（比 1.5MB 再少 3 倍），
-- 縮小被灌爆儲存額度的風險。正常使用不受影響。
-- ============================================================

update storage.buckets set file_size_limit = 524288 where id = 'covers';
